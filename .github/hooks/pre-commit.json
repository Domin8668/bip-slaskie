{
    "hooks": {
        "SessionStart": [
            {
                "type": "command",
                "command": "/home/ds/git/bip-slaskie/.venv/bin/pre-commit install",
                "timeout": 30
            }
        ],
        "PostToolUse": [
            {
                "type": "command",
                "command": "bash -c '[ -z \"$TOOL_INPUT_FILE_PATH\" ] && exit 0; /home/ds/git/bip-slaskie/.venv/bin/pre-commit run --files \"$TOOL_INPUT_FILE_PATH\" > /tmp/pc_out.txt 2>&1; code=$?; if [ $code -ne 0 ]; then cat /tmp/pc_out.txt >&2; exit 2; fi'",
                "timeout": 60
            }
        ]
    }
}
